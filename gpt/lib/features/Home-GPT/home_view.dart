import 'package:flutter/material.dart';
import 'package:gpt/features/Home-GPT/widgets/Home_body.dart';

class HomeView extends StatelessWidget {
  const HomeView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: HomeViewBody(),
    );
  }
}
